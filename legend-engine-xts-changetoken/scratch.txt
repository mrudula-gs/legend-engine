
  // Debug: Log type information to inspect its structure
  console.debug('Debugging type in buildDefaultInstanceValue:', {
    type,
    path,
    isGenericType: type instanceof GenericType,
    rawType: type instanceof GenericType ? type.rawType : null,
    isRawTypeClass: type instanceof GenericType ? type.rawType instanceof Class : false,
    rawTypePath: type instanceof GenericType && type.rawType instanceof Class ? type.rawType.path : null,
  });
